@model Proje.Models.IndexViewModel

<section id="hero">
    <div class="hero-container">
        <div id="heroCarousel" data-bs-interval="5000" class="carousel slide carousel-fade" data-bs-ride="carousel">

            <ol class="carousel-indicators" id="hero-carousel-indicators"></ol>

            <div class="carousel-inner" role="listbox">

                @if (Model.Sliders.Count()>0)
                {
                    
              
                @foreach (var item in Model.Sliders)
                {


                    <div class="carousel-item active" style="background-image:url(assets/img/slide/@item.Image);">
                        <div class="carousel-container">
                            <div class="carousel-content">
                                <h2 class="animate__animated animate__fadeInDown"><span>@item.Baslik</span></h2>
                                <p class="animate__animated animate__fadeInUp">@item.Aciklama</p>
                                <div>
                                    <a asp-action="Index" asp-controller="Menu" class="btn-menu animate__animated animate__fadeInUp scrollto">Menü</a>

                                </div>
                            </div>
                        </div>
                    </div>
                }
                }

            </div>

            <a class="carousel-control-prev" href="#heroCarousel" role="button" data-bs-slide="prev">
                <span class="carousel-control-prev-icon bi bi-chevron-left" aria-hidden="true"></span>
            </a>

            <a class="carousel-control-next" href="#heroCarousel" role="button" data-bs-slide="next">
                <span class="carousel-control-next-icon bi bi-chevron-right" aria-hidden="true"></span>
            </a>

        </div>
    </div>
</section>