@model Proje.Models.IndexViewModel
@{
    Layout = "~/Views/Shared/Admin/Admin_Layout.cshtml";

}
<div class="container-xxl flex-grow-1 container-p-y">

    <div class="card">
        <h5 class="card-header">Neden Biz ?  </h5>
        @if (TempData["Alert"] != null)
        {
            <div class="alert alert-success">
                @TempData["Alert"]
            </div>
        }
        <div class="table-responsive text-nowrap">
            <table class="table table-hover">
                <thead class="table-light">
                    <tr>
                        <th>Başlık</th>
                        <th>Açıklama</th>
                        <th>Düzenle</th>
                    </tr>
                </thead>
                <tbody class="">
                    @foreach (var item in Model.WhyUs)
                    {
                        <tr>

                            <td>@item.Baslik</td>
                            <td>@item.Aciklama</td>
                            <td>
                                <a class="dropdown-item" asp-controller="Admin" asp-action="WhyUsDuzenle" asp-route-id="@item.Id"><i class="bx bx-edit-alt me-1"></i>Düzenle</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>